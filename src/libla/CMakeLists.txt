#

set(local_PREFIX litetpqz_la) # keep cmake target names unique

set(${local_PREFIX}_SOURCES

	# x.cpp

	)

set(${local_PREFIX}_INCLUDES

	eigen.h
	la.h

	eigen.inl

	)

include(../cmakeLibs.cmake)

